export type Job = {
  skills: any;
  id: number;
  title: string;
  company: string;
  description: string;
  url: string;
  skillList: string;
  todo: string;
};

export type Skill = {
  idCode: string;
  name: string;
  url: string;
  description: string;
}
